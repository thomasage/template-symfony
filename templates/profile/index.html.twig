{% extends 'base.html.twig' %}

{% trans_default_domain 'profile' %}

{% block body %}

    <h1>{{ 'page_title'|trans }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div>
        <button type="submit">{{ 'actions.save'|trans(domain: 'messages') }}</button>
    </div>
    {{ form_end(form) }}

    <div>
        <a href="{{ path('app_profile_2fa_setup') }}">Setup 2FA</a>
    </div>

{% endblock %}
