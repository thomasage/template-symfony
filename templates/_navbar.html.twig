<nav class="navbar navbar-expand-lg bg-body-tertiary mb-3">
    <div class="container">
        <a href="{{ path('app_homepage') }}" class="navbar-brand">Acme</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a href="{{ path('app_homepage') }}" class="nav-link active" aria-current="page">Dashboard</a>
                </li>
                <li class="nav-item dropdown">
                    <a href="{{ path('admin') }}" class="nav-link dropdown-toggle" role="button"
                       data-bs-toggle="dropdown" aria-expanded="false">
                        Admin
                    </a>
                    <ul class="dropdown-menu">
                        <li>
                            <a href="{{ ea_url()
                                .setController('App\\Controller\\Admin\\UserCrudController')
                                .setAction('index')
                            }}" class="dropdown-item">Users</a>
                        </li>
                    </ul>
                </li>
            </ul>
            <ul class="navbar-nav mb-2 mb-lg-0">
                <li class="nav-item">
                    <a href="{{ path('app_logout') }}" class="nav-link">Sign out</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
